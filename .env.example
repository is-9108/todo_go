# デプロイ用（プロジェクトルート）
# コピーして .env を作成: cp .env.example .env
# Wi-Fi と VPN 両方からアクセスする場合は両方のオリジンとURLを設定

# フロントエンドのオリジン（CORS用・カンマ区切り）
# 例: Wi-Fi http://192.168.1.100:3000 + VPN http://10.0.0.5:3000
CORS_ORIGINS=http://192.168.1.100:3000,http://10.0.0.5:3000

# APIのURL（ビルド時のフォールバック・フロントエンドはアクセス元ホストを自動使用）
NEXT_PUBLIC_API_URL=http://localhost:8080
